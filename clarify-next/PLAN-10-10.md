# Plano Clarify — Roadmap para 10/10

## Nota Atual: 6,6 / 10,0

| Critério | Nota Atual | Meta | Recuperar |
|---|---|---|---|
| 1. Arquitetura e Organização | 1,7 | 2,0 | +0,3 |
| 2. Backend e Autenticação Supabase | 1,6 | 2,0 | +0,4 |
| 3. Componentes React e UI | 1,5 | 2,0 | +0,5 |
| 4. Formulários e Validação | 0,8 | 2,0 | +1,2 |
| 5. Gerência de Estado | 1,0 | 2,0 | +1,0 |

---

## Critério 4 — Formulários e Validação (0,8 → 2,0)

### 4.1 Instalar dependências
- [ ] `npm install react-hook-form zod @hookform/resolvers`
- [ ] Confirmar que estao em `package.json` dependencies

### 4.2 Criar pasta `src/schemas/` com schemas Zod centralizados
- [ ] `src/schemas/auth.ts`
  - [ ] `loginSchema`: matricula (RegExp `^\d+$`), senha (min 6 chars)
  - [ ] `registroSchema`: nome (min 3), email (RegExp de `utils.ts`), matricula (RegExp `^\d+$`), senha (min 6), chaveAtivacao (obrigatorio)
- [ ] `src/schemas/demandas.ts`
  - [ ] `novaDemandaSchema`: tipo (enum dos TIPOS_DEMANDA), descricao (min 10, max 500)
  - [ ] `feedbackSchema`: texto (min 5)
- [ ] `src/schemas/turmas.ts`
  - [ ] `criarTurmaSchema`: nome (min 3), disciplina (min 3), alunos (array de strings, default [])
- [ ] `src/schemas/usuarios.ts`
  - [ ] `adicionarAlunoSchema`: nome, matricula (RegExp `^\d+$`), email (RegExp), senha (min 6)
- [ ] Importar e reutilizar `validarEmail`, `validarMatricula` de `src/lib/utils.ts` dentro dos `.refine()` do Zod

### 4.3 Refatorar formulários com React Hook Form + ZodResolver
- [ ] `src/app/login/page.tsx`
  - [ ] Trocar `FormData` por `useForm({ resolver: zodResolver(loginSchema) })`
  - [ ] Adicionar `register()` em cada input
  - [ ] Mostrar `formState.errors.campo?.message` abaixo de cada field
  - [ ] Validacao em tempo real (mode: `onChange` ou `onBlur`)
- [ ] `src/app/registro/page.tsx`
  - [ ] Mesma refatoracao com `registroSchema`
  - [ ] Mensagens de erro por campo
- [ ] `src/components/demandas/ModalNovaDemanda.tsx`
  - [ ] Trocar `useState` por `useForm` + `novaDemandaSchema`
  - [ ] Manter contador de caracteres usando `watch('descricao')` do RHF
  - [ ] Manter estados `is-near`/`is-over` do contador
- [ ] `src/components/coordenador/ModalFeedback.tsx`
  - [ ] `useForm` + `feedbackSchema`
- [ ] `src/components/coordenador/ModalCriarTurma.tsx`
  - [ ] `useForm` + `criarTurmaSchema`
  - [ ] Usar `useFieldArray` para lista de matriculas de alunos
- [ ] `src/app/(dashboard)/dashboardcoord/page.tsx` (view adicionar)
  - [ ] `useForm` + `adicionarAlunoSchema` no form de adicionar aluno

### 4.4 Conectar validações RegExp existentes
- [ ] Garantir que `validarEmail` e `validarMatricula` de `src/lib/utils.ts` sejam usados nos schemas Zod via `.refine()`
- [ ] Remover atributo `required` do HTML onde RHF+Zod ja valida

---

## Critério 5 — Gerência de Estado (1,0 → 2,0)

### 5.1 Instalar e configurar TanStack Query
- [ ] `npm install @tanstack/react-query`
- [ ] Criar `src/lib/queryClient.ts` com config (staleTime, refetchOnWindowFocus)
- [ ] Adicionar `QueryClientProvider` no `src/app/layout.tsx` envolvendo `AuthProvider`

### 5.2 Migrar hooks para TanStack Query
- [ ] `src/hooks/useDemandas.ts`
  - [ ] `useQuery({ queryKey: ['demandas', alunoId, status], queryFn })` para listagem
  - [ ] `useMutation` para `criar` com `onSuccess: () => queryClient.invalidateQueries(['demandas'])`
  - [ ] `useMutation` para `atualizarStatus` com optimistic update via `onMutate`
  - [ ] `useQuery` para `buscarPorProtocolo` (queryKey `['demandas', protocolo]`)
  - [ ] **Remover** `refreshKey` state e `recarregar()`
  - [ ] Substituir `recarregar()` por `queryClient.invalidateQueries(['demandas'])`
- [ ] `src/hooks/useTurmas.ts`
  - [ ] `useQuery(['turmas'])` para listagem
  - [ ] `useMutation` para `criar`, `deletar`, `adicionarAluno`, `removerAluno`
  - [ ] Invalidar `['turmas']` apos cada mutacao
  - [ ] **Remover** `refreshKey`
- [ ] `src/hooks/useUsuarios.ts`
  - [ ] `useQuery(['usuarios'])` para listagem
  - [ ] `useMutation` para `adicionar`, `atribuir`, `deletar`
  - [ ] Invalidar `['usuarios']` apos mutacoes
  - [ ] **Remover** `refreshKey`

### 5.3 Atualizar consumers dos hooks
- [ ] `centraldemandas/page.tsx`: adaptar para nova API dos hooks (sem `recarregar`, usa invalidacao automatica)
- [ ] `dashboardcoord/page.tsx`: idem
- [ ] Verificar que `recarregarDemandas()` em dashboardcoord e substituido por invalidacao do TanStack

### 5.4 Instalar e configurar Zustand
- [ ] `npm install zustand`
- [ ] Criar `src/store/uiStore.ts`
  - [ ] Estado: `drawerOpen: boolean`, `toggleDrawer()`, `setDrawer()`
  - [ ] Estado: modal generico (`modalType`, `modalData`)
- [ ] Criar `src/store/filtrosStore.ts`
  - [ ] Estado: `busca: string`, `filtroStatus: StatusDemanda | 'todos'`
  - [ ] Actions: `setBusca()`, `setFiltroStatus()`, `resetFiltros()`
  - [ ] Usar `persist` middleware para manter filtros entre navegacoes

### 5.5 Migrar estado de páginas para stores
- [ ] `centraldemandas/page.tsx`: mover `busca` e `filtroStatus` para `filtrosStore`
- [ ] `(dashboard)/layout.tsx`: mover `drawerOpen` para `uiStore`
- [ ] Mover estados de modais (`modalNovaAberta`, `modalDetalhesAberta`, etc.) para `uiStore` onde fizer sentido

### 5.6 Integrar AuthContext com TanStack Query
- [ ] Transformar `fetchProfile` em `useQuery(['perfil', userId])` com cache automatico
- [ ] Remover chamada manual redundante de `fetchProfile` no `login` (o `onAuthStateChange` ja captura)

---

## Critério 3 — Componentes React e UI (1,5 → 2,0)

### 3.1 Instalar shadcn/ui
- [ ] `npx shadcn@latest init` (configurar com brand orange)
- [ ] `npx shadcn@latest add button input textarea select badge card label dialog`
- [ ] Configurar `components.json` apontando para `src/components/ui`

### 3.2 Criar/migrar componentes base
- [ ] Componente `Button` (variantes: primary=brand-orange, ghost, destructive) substituindo `.modal-btn-primary` e `.modal-btn-ghost`
- [ ] Componente `Input` substituindo `.modal-input` e inputs inline dos forms
- [ ] Componente `Textarea` substituindo `.modal-textarea`
- [ ] Componente `Badge` para status badges (mapear `obterCorStatus` para variant do Badge)
- [ ] Componente `Card` para `CardMetrica`, `CardDemanda`, `CardAluno`, `CardTurma`
- [ ] Componente `Label` substituindo `.modal-label`

### 3.3 Migrar Modal para shadcn Dialog
- [ ] Refatorar `src/components/ui/Modal.tsx` para usar `<Dialog>` do shadcn/ui como base
- [ ] Manter animacoes customizadas (modal-rise-in, etc.) via `data-[state=open]:animate-*`
- [ ] Ou criar Wrapper compativel se manter a API atual `{ open, onClose, children }`

### 3.4 Migrar modais e formulários para componentes shadcn
- [ ] `ModalNovaDemanda.tsx`: usar `<Button>`, `<Input>`, `<Textarea>`, `<Label>`
- [ ] `ModalFeedback.tsx`: idem
- [ ] `ModalCriarTurma.tsx`: idem
- [ ] `ModalDetalhesDemanda.tsx`: usar `<Badge>`, `<Card>`
- [ ] `CardDemanda.tsx`: usar `<Badge>`, `<Card>`
- [ ] `CardMetrica.tsx`: usar `<Card>`
- [ ] `CardAluno.tsx`: usar `<Card>`, `<Badge>`

### 3.5 Limpar `globals.css`
- [ ] Remover `.modal-input`, `.modal-textarea`, `.modal-label` (substituidos por componentes shadcn)
- [ ] Remover `.modal-btn-primary`, `.modal-btn-ghost`, `.modal-close` (substituidos por Button)
- [ ] Remover `.modal-eyebrow-dot`, `.modal-quote` se aplicavel
- [ ] Manter: animacoes keyframe (`animate-cubes`, `pulse-glow`, `float-slow`, `modal-fade-*`, `modal-rise-*`, `modal-stagger`)
- [ ] Manter: `data-reveal`, `text-gradient-warm`, `bg-dot-grid`, `bg-radial-warm`, `shadow-soft-*`
- [ ] Meta: reduzir de ~420 linhas para ~80-100

### 3.6 Manter design system custom
- [ ] Garantir que `tailwind.config.ts` mantem `brand` colors
- [ ] Garantir que font Space Grotesk permanece
- [ ] Garantir responsividade (sm:, md:, lg:) em todos os componentes migrados

---

## Critério 1 — Arquitetura e Organização (1,7 → 2,0)

### 1.1 Remover codigo legado redundante
- [ ] Deletar `src/lib/auth.ts` (sistema `cachedUsuario`/`authListeners` redundante com AuthContext)
- [ ] Migrar funcoes ainda usadas de `lib/auth.ts`:
  - [ ] `usuarioExiste` → mover para `src/lib/perfis.ts` ou hook
  - [ ] `acharUsuario` → mover para `src/lib/perfis.ts` ou hook
  - [ ] `atribuirAluno` → mover para `src/lib/perfis.ts` ou hook
  - [ ] `deletarAluno` → mover para `src/lib/perfis.ts` ou hook
- [ ] Buscar referencias a `lib/auth.ts` no codigo e atualizar imports

### 1.2 Criar estrutura de schemas (ja no Critério 4)
- [ ] Confirmar pasta `src/schemas/` criada e importada em todos os forms
- [ ] Garantir separacao clara: `types/` (interfaces TS), `schemas/` (validacao Zod), `lib/` (logica)

### 1.3 Refatorar register route
- [ ] `src/app/api/auth/register/route.ts`: Trocar `createClient()` inline por `createAdminClient()` de `src/lib/supabase/admin.ts`
- [ ] Remover import direto de `@supabase/supabase-js` na route

### 1.4 Mover fetch espalhado para hooks
- [ ] `centraldemandas/page.tsx` linhas 46-69: fetch direto de `/api/perfis/${demandaDetalhes.alunoId}`
  - [ ] Criar hook `usePerfil(id)` com TanStack Query ou adicionar metodo no `useUsuarios`
  - [ ] Substituir `useEffect + fetch` pelo hook
- [ ] `dashboardcoord/page.tsx` linhas 42-48: `usuariosHook.obterAlunosDoCoordenador` via fetch manual
  - [ ] Mover para `useQuery(['alunos', coordenadorId])` no hook useUsuarios

---

## Critério 2 — Backend e Autenticação Supabase (1,6 → 2,0)

### 2.1 Trocar admin client por server client onde RLS aplica
- [ ] `src/app/api/demandas/route.ts`
  - [ ] GET: usar `createClient()` (server) em vez de `createAdminClient()`
  - [ ] POST: usar `createClient()` (server) — RLS ja permite insert para proprio aluno
- [ ] `src/app/api/demandas/[protocolo]/route.ts`
  - [ ] GET: usar `createClient()` (server)
  - [ ] PATCH: usar `createClient()` (server) — RLS ja permite update para coordenador vinculado
- [ ] `src/app/api/turmas/route.ts`
  - [ ] GET: usar `createClient()` (server)
  - [ ] POST: usar `createClient()` (server)
- [ ] `src/app/api/turmas/[id]/route.ts` DELETE: usar `createClient()` (server)
- [ ] `src/app/api/turmas/[id]/alunos/route.ts` POST: usar `createClient()` (server)
- [ ] `src/app/api/turmas/[id]/alunos/[alunoId]/route.ts` DELETE: usar `createClient()` (server)
- [ ] `src/app/api/perfis/route.ts` GET: usar `createClient()` (server)
- [ ] `src/app/api/perfis/[id]/route.ts` GET + PATCH: usar `createClient()` (server)
- [ ] Manter `createAdminClient()` so em:
  - `perfis/route.ts` POST (createUser) e DELETE (deleteUser)
  - `auth/register/route.ts` (createUser)

### 2.2 Adicionar whitelist de campos no PATCH de perfis
- [ ] `src/app/api/perfis/[id]/route.ts` PATCH:
  ```ts
  const allowed = ['coordenador_id', 'nome']
  const updateData = Object.fromEntries(
    Object.entries(body).filter(([k]) => allowed.includes(k))
  )
  ```
- [ ] Retornar dados atualizados apos update (fazer select)

### 2.3 Adicionar tratamento de erro nas API routes
- [ ] `src/app/api/turmas/route.ts` POST: verificar `error` do insert, retornar 500 com mensagem
- [ ] `src/app/api/turmas/[id]/route.ts` DELETE: verificar `error`
- [ ] `src/app/api/turmas/[id]/alunos/route.ts` POST: verificar `error`
- [ ] `src/app/api/perfis/[id]/route.ts` PATCH: verificar `error` e retornar dados
- [ ] Padronizar formato de erro: `{ ok: false, mensagem: string }`

### 2.4 Remover logica duplicada no AuthContext
- [ ] `src/context/AuthContext.tsx` `login()`: remover chamada manual de `fetchProfile(session.user.id)` apos login
  - [ ] O `onAuthStateChange` ja captura a sessao nova e chama `fetchProfile` automaticamente
  - [ ] Manter so o `await supabase.auth.getSession()` para confirmar sessao antes de retornar

---

## Ordem de Execução Recomendada

1. **Critério 5.1** — Instalar TanStack Query + QueryClientProvider
2. **Critério 5.4** — Instalar Zustand + criar stores
3. **Critério 4.1** — Instalar react-hook-form + zod
4. **Critério 4.2** — Criar schemas Zod em `src/schemas/`
5. **Critério 3.1** — Instalar shadcn/ui
6. **Critério 4.3** — Refatorar formularios com RHF + Zod
7. **Critério 5.2** — Migrar hooks para TanStack Query
8. **Critério 5.5** — Migrar estado de páginas para Zustand stores
9. **Critério 3.2 + 3.4** — Criar e migrar componentes shadcn/ui
10. **Critério 3.5** — Limpar globals.css
11. **Critério 1.1** — Remover lib/auth.ts legado
12. **Critério 1.4** — Mover fetch espalhado para hooks
13. **Critério 2.1** — Trocar admin client por server client
14. **Critério 2.2 + 2.3** — Whitelist + tratamento de erro
15. **Critério 2.4** — Remover fetchProfile duplicado no AuthContext
16. **Critério 1.3** — Refatorar register route

---

## Notas

- Sempre rodar `npm run lint` apos cada bloco de mudancas
- Sempre rodar `npm run build` para verificar tipos
- Priorizar nao quebrar funcionalidades existentes durante migracao
- Commitar apos cada criterio concluido para facilitar rollback