# Plano Clarify — Roadmap para 10/10

## Nota Atual: 8,2 / 10,0 (revisado 27/06/2026)

| Critério | Nota Anterior | Nota Atual | Meta | Recuperar |
|---|---|---|---|---|
| 1. Arquitetura e Organização | 1,7 | 1,9 | 2,0 | +0,2 |
| 2. Backend e Autenticação Supabase | 1,6 | 1,7 | 2,0 | +0,1 |
| 3. Componentes React e UI | 1,5 | 1,7 | 2,0 | +0,2 |
| 4. Formulários e Validação | 0,8 | 2,0 | 2,0 | — |
| 5. Gerência de Estado | 1,0 | 1,9 | 2,0 | +0,1 |

> **Nota anterior:** 6,6/10 → **Nota atual:** 8,2/10 (+1,6)

---

## Critério 4 — Formulários e Validação (0,8 → 2,0) ✅ CONCLUÍDO

### 4.1 Instalar dependências
- [x] `npm install react-hook-form zod @hookform/resolvers`
- [x] Confirmar que estao em `package.json` dependencies

### 4.2 Criar pasta `src/schemas/` com schemas Zod centralizados
- [x] `src/schemas/auth.ts`
  - [x] `loginSchema`: matricula (RegExp `^\d+$`), senha (min 6 chars)
  - [x] `registroSchema`: nome (min 3), email (RegExp de `utils.ts`), matricula (RegExp `^\d+$`), senha (min 6), chaveAtivacao (obrigatorio)
- [x] `src/schemas/demandas.ts`
  - [x] `novaDemandaSchema`: tipo (enum dos TIPOS_DEMANDA), descricao (min 10, max 500)
  - [x] `feedbackSchema`: texto (min 5)
- [x] `src/schemas/turmas.ts`
  - [x] `criarTurmaSchema`: nome (min 3), disciplina (min 3), alunos (array de strings, default [])
- [x] `src/schemas/usuarios.ts`
  - [x] `adicionarAlunoSchema`: nome, matricula (RegExp `^\d+$`), email (RegExp), senha (min 6)
- [x] Importar e reutilizar `validarEmail`, `validarMatricula` de `src/lib/utils.ts` dentro dos `.refine()` do Zod

### 4.3 Refatorar formulários com React Hook Form + ZodResolver
- [x] `src/app/login/page.tsx` — RHF + zodResolver(loginSchema), register() em inputs, erros por campo, onChange
- [x] `src/app/registro/page.tsx` — RHF + registroSchema, erros por campo
- [x] `src/components/demandas/ModalNovaDemanda.tsx` — useForm + novaDemandaSchema, watch('descricao') com contador
- [x] `src/components/coordenador/ModalFeedback.tsx` — useForm + feedbackSchema
- [x] `src/components/coordenador/ModalCriarTurma.tsx` — useForm + criarTurmaSchema
- [x] `src/app/(dashboard)/dashboardcoord/page.tsx` — FormAdicionarAluno com useForm + adicionarAlunoSchema

### 4.4 Conectar validações RegExp existentes
- [x] Garantir que `validarEmail` e `validarMatricula` de `src/lib/utils.ts` sejam usados nos schemas Zod via `.refine()`
- [x] Remover atributo `required` do HTML onde RHF+Zod ja valida

---

## Critério 5 — Gerência de Estado (1,0 → 1,9)

### 5.1 Instalar e configurar TanStack Query
- [x] `npm install @tanstack/react-query`
- [x] Criar `src/lib/queryClient.ts` com config (staleTime, refetchOnWindowFocus)
- [x] Adicionar `QueryClientProvider` no `src/components/providers/QueryProvider.tsx`

### 5.2 Migrar hooks para TanStack Query
- [x] `src/hooks/useDemandas.ts` — useQuery + useMutation com optimistic update + invalidateQueries
- [x] `src/hooks/useTurmas.ts` — useQuery + useMutation para criar, deletar, adicionar/remover aluno
- [x] `src/hooks/useUsuarios.ts` — useQuery + useMutation
- [x] `src/hooks/usePerfil.ts` — useQuery por ID

### 5.3 Atualizar consumers dos hooks
- [x] `centraldemandas/page.tsx` — adaptado para TanStack Query
- [x] `dashboardcoord/page.tsx` — adaptado para TanStack Query

### 5.4 Instalar e configurar Zustand
- [x] `npm install zustand`
- [x] Criar `src/store/uiStore.ts` — drawerOpen, toggleDrawer, setDrawer
- [x] Criar `src/store/filtrosStore.ts` — busca, filtroStatus, persist middleware

### 5.5 Migrar estado de páginas para stores
- [x] `centraldemandas/page.tsx` — busca e filtroStatus no filtrosStore
- [x] `(dashboard)/layout.tsx` — drawerOpen no uiStore

### 5.6 Integrar AuthContext com TanStack Query
- [ ] Transformar `fetchProfile` em `useQuery(['perfil', userId])` com cache automatico
- [ ] Remover chamada manual redundante de `fetchProfile` no login

> **Pendente:** 5.6 — otimização do fetchProfile no AuthContext (ganho marginal, +0,1)

---

## Critério 3 — Componentes React e UI (1,5 → 1,7)

### 3.1 Instalar shadcn/ui
- [x] `components.json` configurado com brand orange
- [x] Componentes base instalados via Radix UI (Dialog) + Class Variance Authority (Badge, Card)
- [ ] Button, Input, Textarea, Select como componentes shadcn completos (forms ainda usam Tailwind direto)

### 3.2 Criar/migrar componentes base
- [x] Componente `Badge` — cva variants mapeadas de `obterCorStatus`
- [x] Componente `Card` — wrapper cva
- [x] Componente `Dialog` — wrapper do @radix-ui/react-dialog
- [ ] Componente `Button` com variantes (primary, ghost, destructive)
- [ ] Componente `Input` padronizado
- [ ] Componente `Textarea` padronizado

### 3.3 Migrar Modal para shadcn Dialog
- [x] `src/components/ui/Dialog.tsx` usa `<Dialog>` do Radix como base

### 3.4 Migrar modais e formulários para componentes shadcn
- [x] `ModalDetalhesDemanda.tsx` — usa `<Badge>`, `<Card>`
- [x] `CardDemanda.tsx` — usa `<Badge>`, `<Card>`
- [x] `CardMetrica.tsx` — usa `<Card>`
- [x] `CardAluno.tsx` — usa `<Card>`, `<Badge>`
- [ ] Forms (login, registro, modais) — ainda usam classes Tailwind direto em vez de `<Button>`, `<Input>`, `<Textarea>`

### 3.5 Limpar `globals.css`
- [ ] Remover `.modal-input`, `.modal-textarea`, `.modal-label` (substituidos por componentes shadcn)
- [ ] Manter animacoes e utilitários (~80-100 linhas)

### 3.6 Manter design system custom
- [x] `tailwind.config.ts` mantem brand colors (#ca5f15)
- [x] Font Space Grotesk mantida
- [x] Responsividade mantida

> **Pendente:** 3.1 (Button/Input/Textarea), 3.4 (migrar forms), 3.5 (limpar globals.css). Ganho: +0,3

---

## Critério 1 — Arquitetura e Organização (1,7 → 1,9)

### 1.1 Remover codigo legado redundante
- [x] Deletar `src/lib/auth.ts` — já removido
- [x] Funções migradas para API routes (perfis, auth)
- [x] Imports atualizados

### 1.2 Criar estrutura de schemas
- [x] Pasta `src/schemas/` criada e importada em todos os forms
- [x] Separação: `types/` (interfaces TS), `schemas/` (validação Zod), `lib/` (utilitários)

### 1.3 Refatorar register route
- [ ] `src/app/api/auth/register/route.ts` — verificar se usa `createAdminClient()` de `src/lib/supabase/admin.ts` (pode já estar feito)

### 1.4 Mover fetch espalhado para hooks
- [x] `usePerfil(id)` — hook com TanStack Query
- [ ] `useUsuarios` — alguns fetches inline de perfis podem ainda existir nos componentes

> **Pendente:** 1.3 (verificar register route), 1.4 (fetches residuais). Ganho: +0,1

---

## Critério 2 — Backend e Autenticação Supabase (1,6 → 1,7)

### 2.1 Trocar admin client por server client onde RLS aplica
- [ ] Várias API routes — verificar quais ainda usam `createAdminClient()` onde `createClient()` (server) bastaria com RLS

### 2.2 Adicionar whitelist de campos no PATCH de perfis
- [ ] `src/app/api/perfis/[id]/route.ts` PATCH — filtrar apenas campos permitidos

### 2.3 Adicionar tratamento de erro nas API routes
- [ ] Verificar `error` nos inserts/updates/deletes, retornar 500 com mensagem
- [ ] Padronizar formato de erro: `{ ok: false, mensagem: string }`

### 2.4 Remover logica duplicada no AuthContext
- [ ] `AuthContext.login()` — possível chamada duplicada de fetchProfile

> **Pendente:** 2.1-2.4 — refinamentos de segurança e robustez. Ganho: +0,3

---

## Próximos Passos (para 10/10)

### O que foi feito (desde a nota 6,6)
- ✅ Migração completa para Supabase (Auth, PostgreSQL, RLS, 11 API routes)
- ✅ React Hook Form + Zod em todos os formulários
- ✅ TanStack React Query em todos os hooks de dados
- ✅ Zustand para estado de UI (uiStore, filtrosStore)
- ✅ Schemas Zod centralizados em `src/schemas/`
- ✅ `lib/auth.ts` removido
- ✅ Landing page completa (310 linhas)

### O que falta (ganho restante: +1,8)

| # | Tarefa | Critério | Ganho |
|---|--------|----------|-------|
| 1 | Criar Button, Input, Textarea shadcn e migrar forms | 3 | +0,15 |
| 2 | Limpar globals.css (remover classes .modal-*) | 3 | +0,10 |
| 3 | Migrar modais restantes para shadcn | 3 | +0,05 |
| 4 | Trocar admin client por server client nas API routes (RLS) | 2 | +0,20 |
| 5 | Adicionar whitelist no PATCH perfis + tratamento de erro | 2 | +0,10 |
| 6 | Otimizar fetchProfile no AuthContext (TanStack Query) | 5 | +0,10 |
| 7 | Remover fetches inline residuais nos componentes | 1 | +0,10 |

---

## Notas

- Sempre rodar `npm run lint` apos cada bloco de mudancas
- Sempre rodar `npm run build` para verificar tipos
- Priorizar nao quebrar funcionalidades existentes durante migracao
- Commitar apos cada criterio concluido para facilitar rollback